import React from 'react';
import ErrorIndicator from '../error-indicator';
import Spinner from '../spinner';

const App = () => {
  return (
    <div>
      <ErrorIndicator />
      <Spinner />
    </div>
  );
};

export default App;
